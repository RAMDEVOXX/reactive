spring:
  application:
    name: re@ctive-app
  profiles:
    active: dev
    cloud:
      vault:
        kv:
          enabled: false
  cloud:
    config:
      uri: http://localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always